server {
    listen 80;
    server_name civicedge.site www.civicedge.site;

    # frontend
    root /usr/share/nginx/html;
    index index.html;

    # Django Admin
    location /admin/ {
        proxy_pass http://django:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # API → Django
    location /api/ {
        proxy_pass http://django:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # React routing
    location / {
        try_files $uri /index.html;
    }
}